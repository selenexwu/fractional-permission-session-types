type nat = +{zero : 1,
             succ : nat}

proc zero : . |- (n : nat) =
  n.zero;
  close n

proc succ[ni] : (n : <nat,*,ni>) |- (n1 : nat) =
  n1.succ;
  n1 <-> n

type A = 1

type queue = /\k. &{ins : \\// !a. <A,*,a> -o queue,
                    del : \\// +{none : 1,
                                 some : ?a. <A,*,a> * queue},
                    len : ?a. <nat,*,a> * \/ queue}

proc empty : . |- (q : queue) =
  immut { p =>
    case q (
      ins => mut {
        x <- recv q;
        e <- empty;
        q <- elem x e
      }
    | del => mut {
        q.none;
        close q
      }
    | len =>
      n0 <- zero;
      send q n0;
      continue
    )
  }

proc elem[xi,ti] : (x : <A,*,xi>), (t : <queue,*,ti>) |- (q : queue) =
  share t;
  immut t { p =>
    case q (
      ins => mut {
        own t;
        y <- recv q;
        t.ins;
        send t y;
        q <- elem x t
      }
    | del => mut {
        own t;
        q.some;
        send q x;
        q <-> t
      }
    | len =>
        t.len;
        n <- recv t;
        n1 <- succ n;
        send q n1;
        continue t
    )
  }

